# config.py no longer contains real secrets (Telegram token/chat ID are read
# from environment variables / GitHub Actions secrets instead) so it's safe
# to commit. trade_state.json / leaderboard.json are intentionally tracked
# too -- the GitHub Actions scanner workflow commits them back to the repo
# after each run so it remembers state between scans.
scanner.log

# Python
__pycache__/
*.pyc
venv/
.venv/

# Streamlit
.streamlit/
